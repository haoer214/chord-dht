# v1-nodeExit
主要功能实现：dht节点主动下线，退出网络；更新全网路由表
具体实现方法：
（1）新建ID=-1线程用来监听节点退出命令，当输入为“exit”时，调用beforeExit()函数，然后结束程序
（2）beforeExit()函数包含功能：更新后继节点的前驱；后继从超级节点获取网络中现存节点信息（计划在去掉超级节点后，每个dht节点保存一份nodeList并随时进行同步更新）；通过后继节点更新全网路由表（遍历每个现存节点的路由表，若其Successor=exitNode，则改为该后继节点）